import * as React from "react";
export interface MarkdownProps {
    value: string;
}
export declare type TableNode = Array<React.ReactElement<{
    children: TableNode;
    value: string;
}>>;
declare const Markdown: ({ value: inputValue }: MarkdownProps) => JSX.Element;
export default Markdown;
