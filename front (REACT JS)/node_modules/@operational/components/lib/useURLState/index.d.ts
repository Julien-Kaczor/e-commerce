import { Dispatch, SetStateAction } from "react";
/**
 * Create a state that is sync with url search param.
 *
 * @param name Name of your state
 * @param decoder Validate and decode the value from the url
 * @param options `window` dependent methods
 */
export declare const useURLState: <T>(name: string, initialValue: T, decoder?: (value: any) => T | undefined, { getHash, getPathname, replaceState, getSearch }?: {
    getSearch: () => string;
    getHash: () => string;
    getPathname: () => string;
    replaceState: any;
} | {
    getSearch: (...args: any[]) => void;
    getHash: (...args: any[]) => void;
    getPathname: (...args: any[]) => void;
    replaceState: (...args: any[]) => void;
}) => [T, Dispatch<SetStateAction<T>>];
