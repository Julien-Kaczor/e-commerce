import React from "react";
import { IconName } from "../Icon/Icon";
export interface Tab {
    name: string;
    children: React.ReactNode;
    hidden?: boolean;
    icon?: IconName;
    iconColor?: string;
    loading?: boolean;
}
export interface Props {
    tabs: Tab[];
    activeTabName?: string;
    onTabChange?: (newTabName: string) => void;
    condensed?: boolean;
    children: (childrenConfig: {
        tabsBar: React.ReactNode;
        activeChildren: React.ReactNode;
    }) => React.ReactNode;
}
export interface State {
    activeTab: number;
}
export declare const tabsBarHeight = 40;
declare const Tabs: ({ onTabChange, tabs, activeTabName, condensed, children }: Props) => JSX.Element;
export default Tabs;
