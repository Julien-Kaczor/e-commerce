import React from "react";
export interface ModalOptions {
    fullSize?: boolean;
    title: React.ReactNode;
    body: React.ReactNode | ((close: () => void) => React.ReactNode);
}
export declare type State = Partial<ModalOptions>;
export interface Props {
    children: (modal: (options: ModalOptions) => void) => React.ReactNode;
}
export declare const Modal: ({ children }: Props) => JSX.Element;
export default Modal;
