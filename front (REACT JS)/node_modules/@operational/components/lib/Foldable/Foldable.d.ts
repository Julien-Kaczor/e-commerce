import * as React from "react";
import { TogglerProps } from "./Foldable.Toggler";
export interface FoldableProps {
    /** Should the foldable children be open or closed by default? */
    initialState?: "closed" | "open";
    children: ({ Toggler, isFolded, }: {
        Toggler: React.ComponentType<TogglerProps>;
        isFolded: boolean;
    }) => React.ReactNode;
}
declare const Foldable: ({ initialState, children }: FoldableProps) => React.ReactNode;
export default Foldable;
