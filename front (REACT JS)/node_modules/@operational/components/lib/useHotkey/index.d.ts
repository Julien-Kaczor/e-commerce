import { RefObject } from "react";
export interface Key {
    /** Keyboard key value */
    key: string;
    /** Optional Ctrl / ⌃ key */
    ctrl?: boolean;
    /** Optional Shift key */
    shift?: boolean;
    /** Optional Alt / ⌥ key */
    alt?: boolean;
    /** Optional meta ⌘ / ⊞ key */
    meta?: boolean;
}
/**
 * Hook for linking some hotkey to a callback function
 *
 * @param containerNode - A ref to an HTMLElement, scoping the hotkey binding
 * @param key - hotkey definition object
 * @param callback- a callback to be invoked when the hotkey is hit
 *
 */
export declare function useHotkey(containerNode: RefObject<HTMLElement>, hotkey: Key, callback: () => void): void;
export default useHotkey;
